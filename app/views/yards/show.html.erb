<%= render 'shared/nav_bar'%>

<%= render partial: 'shared/yard_show', locals: {
        button_text: "Rent Yard",
        url: new_yard_booking_path(@yard.id),
        yard: @yard,
        method: :get
}%>
